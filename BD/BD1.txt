SELECT ename,job 
FROM EMP,DEPT 
WHERE EMP.DEPTNO = DEPT.DEPTNO AND LOC='BOSTON'

SELECT EMPNO,JOB,SAL 
FROM EMP 
WHERE SAL>2500

SELECT ename,job 
FROM EMP,DEPT 
WHERE EMP.DEPTNO = DEPT.DEPTNO AND LOC='BOSTON'

SELECT ENAME,JOB,DNAME 
FROM EMP,DEPT 
WHERE EMP.DEPTNO = DEPT.DEPTNO AND LOC='NEW YORK' AND MGR IS NOT NULL;

SELECT * 
FROM EMP,DEPT 
WHERE EMP.DEPTNO=DEPT.DEPTNO AND DNAME='SALES';

SELECT ename,sal 
FROM EMP,DEPT 
WHERE EMP.DEPTNO = DEPT.DEPTNO AND JOB='CLERK' AND LOC='BOSTON';

SELECT ename,hiredate 
FROM EMP 
WHERE hiredate<'15/01/01';